# MaxDock Stage 2 preview branch

This branch exists only to publish the PR #8 Stage 2 review site under `/stage2-preview/` while keeping the production root identical to `main`.

Do not merge this branch into `main`.