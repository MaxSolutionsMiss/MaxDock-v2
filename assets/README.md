# MaxDock brand assets

This directory follows `docs/MAXDOCK_ARCHITECTURE.md`.

- `logo-knockout.png` — MaxDock badge mark used throughout the application.
- `logo-color.png` — full Max Solutions lockup used on the login page only.

Do not add alternate logo placements or release-numbered copies.
